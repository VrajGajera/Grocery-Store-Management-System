namespace Grosary_net.Models
{
    public enum UserRole
    {
        Admin,
        Customer
    }

    public enum OrderStatus
    {
        Pending,
        Processing,
        Completed,
        Cancelled
    }
}
